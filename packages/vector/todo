
Cleaner urls
- router, module loop: insert collectionName as property of document (transform? orm?)
- use a global root setting instead of hardocoded urls
<li><a href="{{pathFor 'article' _id=this._id }}">{{title}}</a></li>????

Images with their own collection
Enable dot notation inside resource setup
Choose between router data or collectionName inside template helpers/events for updating collections

Additional parameters
- pagination
- search / filter

Controllers
- use extended controlled with shared login
- shared login check
- use multiple render for sidebar and so on

Localization
- i18n integration for labels and other texts
- all text should be dynamic and localizable

UI
- avoid page refresh, smooth animations instead
- use sessions for UI settings?

Docs
- installazione
- setup
- uso

Notifications
  - with links to other pages

Relations
  - use route: :collectionName/:id/:childrenCollectionName/